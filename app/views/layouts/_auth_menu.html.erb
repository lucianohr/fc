<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to(content_for?(:title) ? yield(:title) : "Fluxo de Caixa", root_path) %></h1>
    </li>
  </ul>
  <section class="top-bar-section">
    <!-- Left Nav Section
    <ul class="left">
      <li><a href="#">Left Nav Button</a></li>
    </ul>
    -->
    <!-- Right Nav Section -->
    <ul class="right">
      <li class="has-dropdown">
        <% if usuario_signed_in? %>
          <%= link_to(current_usuario.nome, '#')  %>
          <ul class="dropdown">
            <li><%= link_to(t('devise.registrations.edit.name'), edit_usuario_registration_path) %></li>
            <li><%= link_to(t('devise.sessions.destroy.name'), destroy_usuario_session_path, :method=>'delete') %></li>
          </ul>
        <% else %>
          <%= link_to(t('devise.sessions.new.name'), '#')  %>
          <ul class="dropdown">
            <li><%= link_to(t('devise.sessions.new.name'), new_usuario_session_path) %></li>
            <li><%= link_to(t('devise.registrations.new.name'), new_usuario_registration_path) %></li>
          </ul>
        <% end %>
      </li>
    </ul>
  </section>
</nav>
